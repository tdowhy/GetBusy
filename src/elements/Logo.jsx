import React from 'react';
import classes from './Logo.module.css';

const logo = () => {
    return (
        <h1 className={classes.Logo}>
            GET BUSY
        </h1>
    );
}

export default logo;